import React from "react"
import pin from '../images/pin.png'

const Pin = (props) => {
  return (
  <div className="">
    <img className="pin" src={pin} alt="pin" />
  </div>
  )
}

export default Pin
